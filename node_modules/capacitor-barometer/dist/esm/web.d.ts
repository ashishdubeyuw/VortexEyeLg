import { WebPlugin, PluginListenerHandle } from '@capacitor/core';
import type { Barometer as BarometerInterface } from './definitions';
export declare class BarometerWeb extends WebPlugin implements BarometerInterface {
    constructor();
    isAvailable(): Promise<{
        available: boolean;
    }>;
    start(): Promise<void>;
    stop(): Promise<void>;
    getPressure(): Promise<{
        pressure: number;
    }>;
    echo(options: {
        value: string;
    }): Promise<{
        value: string;
    }>;
    addListener(eventName: 'onPressureChange', _listenerFunc: (data: {
        pressure: number;
    }) => void): Promise<PluginListenerHandle> & PluginListenerHandle;
}
